{
  "name": "@contactsnap/parser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "eval": "vitest run tests/eval.test.ts"
  },
  "dependencies": {
    "@contactsnap/shared-types": "0.1.0",
    "libphonenumber-js": "^1.13.8"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}
